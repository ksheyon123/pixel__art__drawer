import { useState } from "react";

type Coordinate = [number, number];

const useOutliner = () => {

  const [selectedOutline, selectOutline] = useState<Coordinate[]>([]);

  const updateOutline = () => {

  }

  return {

  }
}

export { useOutliner }